# Security Policy

## 敏感信息

API Key 应通过应用内的 **设置中心 → AI** 保存到 Windows Credential Manager。开发或自动化环境可使用本机 `.env`，且环境变量优先于凭据管理器。密钥不会写入 SQLite、JSON 备份、前端缓存或日志。

如果密钥曾进入 Git 历史、Issue、日志或截图，请立即在对应平台撤销并重新生成；仅删除当前文件不足以使旧密钥失效。

## 报告问题

请不要在公开 Issue 中发布有效密钥、个人任务数据库或可利用的漏洞细节。请通过仓库维护者提供的私密安全报告渠道联系；若尚未启用该渠道，请创建一个不含敏感细节的 Issue，请求维护者提供私下联系方式。

Yantu 仅为本机单用户应用，默认只监听 `127.0.0.1`。不要将其反向代理或暴露到公网。

请只从本仓库的 GitHub Releases 下载安装包，并可使用同名 `.sha256` 文件核验下载内容。安装版用户数据默认位于 `%LOCALAPPDATA%\Yantu`，卸载应用不会删除该目录。
